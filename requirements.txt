pypdf>=4.0.0
# ragas currently pulls scikit-network, which has no wheel on Python 3.14 (Windows).
ragas>=0.1.7; python_version < "3.14"
openai>=1.10.0
python-dotenv>=1.0.0
pandas>=2.1.0
tqdm>=4.66.0
jinja2>=3.1.0
